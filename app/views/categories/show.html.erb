<!-- <h1>Categories#show</h1>
<p>Find me in app/views/categories/show.html.erb</p> -->
<!-- <div class="space-btn">
  <div class="back-btn">
    <%#= link_to "Back", category_categories_path(@category.id)%>
  </div>
</div> -->
<!-- <div class="space-bar">
  <div class="back-btn">
    <%#= link_to "Back", categories_path %>
  </div>
  <h2>In <%#= @category.name %></h2>
</div> -->
<div class="sub-c-banner">
  <div class="back-btn">
    <%= link_to "All", best_deals_path %>
  </div>
  <% @categories.each do |category| %>
    <%#= link_to "#{category.name}", category_category_path(@category, category) %>
    <%= category.name %>
    <%#= link_to 'Show products', category_products_path(@category, category.id), method: :get%>
    <%#= link_to 'Show category', category_path(category), method: :get%>
  <% end %>
  <div class="back-btn">
    <%= link_to "Get Lucky!", '#' %>
  </div>
</div>

<!-- <h3>All Products from <%#= @category.name %></h3> -->
<div class="products-grid">
  <% @products.each do |product| %>
    <div class="product-card">
      <%= link_to "#{product.name}", category_product_path(@category, product), method: :get%>
      <%= product.price %>€
    </div>
  <% end %>
</div>
<%#= link_to "New product", new_category_category_product_path(@category, @category) %>

