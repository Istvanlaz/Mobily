<nav>
  <div class="navbar-mobily">
      <ul class="navbar-links">
        <li class="navbar-logo">
          <%= link_to "Mobily.", "#" %>
        </li>
        <li>
          <div class="loginbox" >
            <%= form_for(resource, as: resource, url: session_path(resource)) do |f| %>
              <div class="login-sub">
                <div class="nav-search-btn">
                    <%= f.email_field :email, required: false, autofocus: true, placeholder: "Email", label:false %>
                </div>
                <div class="nav-search-btn">
                    <%= f.password_field :password, required: false, placeholder: "Password", label:false %>
                </div>
                <%= f.submit "Login", class: "login-btn" %>
              </div>
            <% end %>
            <div class="shared-links">
              <%= render "devise/shared/links" %>
            </div>
          </div>
        </li>
      </ul>
  </div>
  <div class="space-btn">...</div>
 <!--  <div class="navbar-login "> -->
  <!-- </div> -->
</nav>

<!-- , input_html: { class: "search-text" } -->
<!-- , input_html: { class: "login-password" } -->

<!-- <div class="loginbox">
            <form action="/resource">
              <input type="text" name="email" placeholder="Email">
              <input type="password" name="password" placeholder="Password">
              <input type="submit" name="login" value="Login">
            </form>
            <%#= render "devise/shared/links" %>
          </div> -->



